{
  "name": "shortid-dist",
  "version": "1.0.4",
  "description": "CDN friendly build of shortid",
  "main": "index.js",
  "scripts": {
    "wp": "webpack",
    "build": "webpack -d && webpack -p",
    "clean": "rimraf dist package shortid-dist-*.tgz",
    "pack": "npm pack --dry-run",
    "repack": "npm run clean && npm run build && npm run pack",
    "links": "node scripts/links.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adamjarret/shortid-dist.git"
  },
  "bugs": {
    "url": "https://github.com/adamjarret/shortid-dist/issues"
  },
  "keywords": [
    "shortid",
    "cdn"
  ],
  "homepage": "https://adamjarret.github.io/shortid-dist",
  "author": "Adam Jarret <adam@atj.me>",
  "license": "0BSD",
  "dependencies": {
    "shortid": "^2.2.13"
  },
  "devDependencies": {
    "license-webpack-plugin": "^1.5.0",
    "rimraf": "^2.6.2",
    "semver": "^5.6.0",
    "webpack": "^4.20.2",
    "webpack-cli": "^3.1.2"
  }
}
